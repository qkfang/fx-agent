name: 42 Deploy Research Analytics

on:
  push:
    branches: [main]
    paths:
      - "src/research-analytics/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        working-directory: src/research-analytics
        run: dotnet restore

      - name: Build
        working-directory: src/research-analytics
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        working-directory: src/research-analytics
        run: dotnet publish --configuration Release --no-build --output ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: research-analytics-dist
          path: src/research-analytics/publish

  deploy-dev:
    needs: build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Azure CLI Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: research-analytics-dist
          path: dist

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: fxag-research
          resource-group-name: rg-fx-agent
          package: dist
