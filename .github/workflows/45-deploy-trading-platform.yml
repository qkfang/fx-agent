name: 45 Deploy Trading Platform

on:
  push:
    branches: [main]
    paths:
      - "src/trading-platform/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        working-directory: src/trading-platform
        run: dotnet restore

      - name: Build
        working-directory: src/trading-platform
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        working-directory: src/trading-platform
        run: dotnet publish --configuration Release --no-build --output ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: trading-platform-dist
          path: src/trading-platform/publish

  deploy-dev:
    needs: build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Azure CLI Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: trading-platform-dist
          path: dist

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: fxag-trading
          resource-group-name: rg-fx-agent
          package: dist
